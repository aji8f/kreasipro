# Security Configuration for Kreasi Pro
# Apache .htaccess file

# Enable RewriteEngine
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
</IfModule>

# ============================================
# URL Rewriting
# ============================================
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Serve sitemap.xml dynamically
    RewriteRule ^sitemap\.xml$ sitemap.xml.php [L]
    
    # Redirect www to non-www (optional)
    # RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
    # RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
</IfModule>

# ============================================
# Security Headers
# ============================================

<IfModule mod_headers.c>
    # Prevent clickjacking attacks
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Prevent MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Enable XSS protection in browsers
    Header always set X-XSS-Protection "1; mode=block"
    
    # Referrer Policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Remove server signature
    Header unset Server
    Header unset X-Powered-By
    
    # HSTS (HTTP Strict Transport Security) - Only enable if using HTTPS
    # Uncomment the line below if your site uses HTTPS
    # Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
</IfModule>

# ============================================
# Disable Directory Listing
# ============================================
Options -Indexes

# ============================================
# File Access Restrictions
# ============================================

# Protect config files
<FilesMatch "^(config\.php|security\.php)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect .htaccess and .htpasswd
<FilesMatch "^\.ht">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect backup files
<FilesMatch "\.(bak|backup|old|orig|save|swp)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect log files
<FilesMatch "\.log$">
    Order allow,deny
    Deny from all
</FilesMatch>

# ============================================
# Block Documentation Files (Internal Only)
# ============================================

# Block all .md files (internal documentation)
<FilesMatch "\.(md|MD)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# If you want to allow specific .md files for public access, add exceptions:
# <Files "PUBLIC_README.md">
#     Allow from all
# </Files>

# ============================================
# Block Test Files
# ============================================

# Block all test files
<FilesMatch "^test_">
    Order allow,deny
    Deny from all
</FilesMatch>

# Specific test file (redundant but explicit)
<Files "test_security.php">
    Order allow,deny
    Deny from all
</Files>

# ============================================
# Block Development/Copy Files
# ============================================

# Block files with copy, temp, tmp, dev, local in name
<FilesMatch "(copy|temp|tmp|dev|local)\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block specific development files
<Files "index copy.php">
    Order allow,deny
    Deny from all
</Files>

# ============================================
# PHP Security Settings
# ============================================

<IfModule mod_php7.c>
    # Disable dangerous PHP functions (if not already disabled in php.ini)
    php_flag display_errors Off
    php_flag log_errors On
    php_flag expose_php Off
    
    # File upload restrictions
    php_value upload_max_filesize 10M
    php_value post_max_size 10M
    
    # Session security
    php_flag session.cookie_httponly On
    php_flag session.use_strict_mode On
</IfModule>

# ============================================
# MIME Type Security
# ============================================

<IfModule mod_mime.c>
    # Force download for potentially dangerous files
    AddType application/octet-stream .exe .dll .bat .sh
    
    # Proper MIME types for common files
    AddType text/html .html .htm
    AddType text/css .css
    AddType application/javascript .js
    AddType image/jpeg .jpg .jpeg
    AddType image/png .png
    AddType image/gif .gif
    AddType image/webp .webp
</IfModule>

# ============================================
# Compression (Optional - for performance)
# ============================================

<IfModule mod_deflate.c>
    # Compress HTML, CSS, JavaScript, Text, XML
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css
    AddOutputFilterByType DEFLATE application/javascript application/json
</IfModule>

# ============================================
# Browser Caching (Optional - for performance)
# ============================================

<IfModule mod_expires.c>
    ExpiresActive On
    
    # Images
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    
    # CSS and JavaScript
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    
    # Fonts
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
</IfModule>

# ============================================
# Error Pages (Optional)
# ============================================

# Custom error pages - uncomment and create these files if needed
# ErrorDocument 404 /404.html
# ErrorDocument 403 /403.html
# ErrorDocument 500 /500.html
